{% for i in 1..5 %}
    <form method="post" action="{{ path('movie_rate1', {'id': movie.id, 'avgRating': i }) }}" onsubmit="return confirm('Are you sure you want to add a rating?');">
        <input type="hidden" name="_method" value="POST">
        <button class="btn">{{ i }}</button>
    </form>
{% endfor %}
